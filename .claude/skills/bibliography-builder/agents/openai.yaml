interface:
  display_name: "Bibliography Builder"
  short_description: "Build or extend BibTeX, reference lists, or footnotes"
  default_prompt: "Use $bibliography-builder to build references from selected PaperHub papers, add them to an existing BibTeX file, or add them to a target Markdown note."
